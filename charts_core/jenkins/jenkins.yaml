name: jenkins
release:
  chart: charts.downup.us/jenkins:0.6.220
  version: 0.6.220
configuration:
  clusterDomain: master.local
secrets:
 - jenkins-admin-user
 - jenkins-admin-password
 - jenkins-github-access-token
 - jenkins-vault-user
 - jenkins-vault-password
